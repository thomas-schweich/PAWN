"""JAX/Equinox implementation of PAWN — pretraining, adapters, and eval.

Migration in progress; see ``docs/jax-migration.md``. During the migration
this package coexists with the PyTorch modules under ``pawn/``; at the end of
the migration PyTorch is removed and this package is flattened up into
``pawn/``.
"""
