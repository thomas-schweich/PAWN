"""Partition C (Python Infra) local fixtures.

Owned by Partition C. Add fixtures here that are shared within
``tests/core/`` but not useful outside (e.g. config factories, synthetic
optimizer state, MetricsLogger tmp paths).
"""
